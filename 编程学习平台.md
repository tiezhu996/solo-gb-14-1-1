<!-- 改编自：教育行业主题项目提示词/gb-14-1；后端由 FastAPI (Python) 改造为 Go 1.22 + Gin + mongo-driver -->
# gb-14-1 编程学习平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述与原始功能需求

##### 项目概述

构建一个面向编程初学者和进阶者的在线编程学习平台，提供系统化的学习路径、在线代码编辑与运行环境、编程题目自动评测功能。用户可在浏览器中直接编写和运行代码，无需本地搭建开发环境，降低编程学习门槛。

##### 功能需求

1. **学习路径管理**：平台提供结构化学习路径（如"Python 入门→Web 开发→数据分析"），每个路径包含多个课程模块，模块包含多个知识点和对应的练习题，用户按路径顺序学习并跟踪进度
2. **课程内容管理**：管理员创建课程（支持 Markdown 格式的图文教程），每个课程包含章节，章节包含知识点讲解和配套练习题，支持代码片段高亮展示
3. **在线 IDE**：浏览器内嵌代码编辑器，支持 Python、JavaScript、Java 三种语言，提供语法高亮和自动补全，代码在服务端沙箱中运行并返回输出结果，运行超时限制 10 秒自动终止
4. **编程题目评测**：支持 ACM 风格评测——用户提交代码后系统运行多个测试用例，判定通过/部分通过/运行错误/超时，显示每个测试用例的输入、期望输出和实际输出对比
5. **排行榜与成就系统**：根据用户解题数量和难度加权计算积分排行榜（日榜、周榜、总榜），设置成就徽章（如"连续签到 7 天"、"完成 100 题"、"首次通过困难题"）
6. **讨论社区**：每道题目下方开设讨论区，用户可发布题解分享和思路讨论，支持按"最佳答案"排序，社区帖子支持 Markdown 格式和代码块
7. **个人学习仪表盘**：展示用户的学习统计数据——累计学习时长、完成课程数、解题总数、各语言解题分布饼图、每日学习热力图（类似 GitHub 贡献图）


### 2. 核心实体与模块拆分（≥ 4 个实体，必须贯穿全栈）

从上面的功能需求中抽象出至少 4 个核心实体（例如：用户、业务主体、订单/预约/记录、资源、评价等）。要求：

- 每个实体必须明确字段、关联关系、状态机与业务动作。
- 每个实体必须同时贯穿：数据库表/集合 → 后端 `backend/internal/model/<entity>.go` → `backend/internal/repository/<entity>_repository.go` → `backend/internal/service/<entity>_service.go` → `backend/internal/handler/<entity>_handler.go` → `backend/internal/router/<entity>.go` → 前端 `api/<entity>.ts`、`stores/<entity>Store.ts`、页面。
- 每个实体的 CRUD 与业务状态流转**严禁合并进单一文件**，必须拆分为 model / dto / repository / service / handler / router / constants 独立文件。
- 多步写操作必须放入 service 层事务中；并发场景必须使用 `SELECT ... FOR UPDATE`（GORM）或 MongoDB 原子操作与唯一索引。


### 3. 核心页面与 API 端点（前端 ≥ 4 页，后端 ≥ 6 个接口，至少 2 个页面/接口复用同一 service 或组件）

- 每个核心页面必须写清楚：路由、消费实体、关键交互、共享组件。
- 前端页面组件、状态管理、API 请求、类型定义必须按功能模块拆分，禁止把所有页面写在一个文件。
- 后端每个模块至少提供对应的 CRUD 接口与业务动作接口，路由前缀统一 `/api/v1`。
- 至少 2 个接口复用同一个 service 方法或 repository 方法，并在接口说明中标注复用关系。


### 4. 横切关注点（≥ 2 个，每个必须写明触达文件层）

至少实现以下 2 项，并逐项写明触达的数据库/后端/前端文件层：

1. **JWT 认证 + RBAC 权限**：数据库角色字段 → `internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/util/jwt.go` → 前端路由守卫与按钮显隐。
2. **操作审计日志**：数据库操作日志表 → `internal/middleware/audit.go` → service 埋点 → 前端审计页面。
3. **全局错误处理与请求追踪**：`internal/middleware/request_id.go`、`internal/middleware/error_handler.go`、`internal/util/app_error.go`、`internal/constants/error_codes.go` → 前端 `utils/request.ts` 拦截器。


### 5. 共享枚举/常量（≥ 2 组）

至少抽象出 2 组业务状态/类型枚举（如订单状态、审核状态、资源类型、角色类型）。要求：

- 后端定义位置：`backend/internal/constants/`；前端定义位置：`frontend/src/constants/`。
- 同一枚举必须同时出现在：模型、DTO、service 状态机、handler 校验、前端筛选/状态徽标、错误码、日志模板中。
- README 必须列出每个枚举在后端的所有出现位置。


### 6. 共享前端组件/hooks（≥ 3 个，全栈项目必须）

- 至少抽象出 3 个跨页面复用的组件，如 `<StatusBadge>`、`<EmptyState>`、`<DataTable>`、`<ConfirmDialog>`。
- 至少 2 个 hooks/utils，如 `useAuth`、`usePagination`、`request.ts`、`format.ts`。
- 组件、hooks、store、api 按实体和功能拆分，禁止全部堆在 `src/` 根目录。

### 7. 后端中间件清单（≥ 2 个）

- 认证中间件、RBAC 权限中间件、请求 ID 中间件、错误恢复中间件、限流中间件、审计日志中间件中至少实现 2 个。
- 每个中间件必须放在 `backend/internal/middleware/` 下的独立文件。

### 8. 技术要求

| 层 | 技术栈 |
| --- | --- |
| 前端 | React + TypeScript，使用 Tailwind CSS 样式框架，Vite 构建工具 |
| 后端 | Go 1.22 + Gin + mongo-driver |
| 数据库 | MongoDB |
| 缓存 | Redis |
| 认证 | JWT + RBAC |
| 日志 | `log/slog` 或 `go.uber.org/zap` |
| 参数校验 | `github.com/go-playground/validator/v10` |
| 接口文档 | Swagger/OpenAPI |

后端本地开发命令：`cd backend && go mod tidy && go run ./cmd/server`；构建命令：`go build ./...`。

### 9. 文件结构强制清单

```
backend/
├── cmd/server/main.go
├── internal/
│   ├── config/config.go
│   ├── database/database.go
│   ├── model/           # 每个实体一个文件
│   ├── dto/             # 每个实体一个 DTO 文件
│   ├── repository/      # 每个实体一个 repository 文件
│   ├── service/         # 每个实体一个 service 文件
│   ├── handler/         # 每个实体一个 handler 文件
│   ├── router/          # 每个实体一个路由注册文件
│   ├── middleware/
│   ├── constants/
│   └── util/
├── pkg/
├── migrations/
├── Dockerfile
├── go.mod
└── go.sum
frontend/
├── src/api/             # 每个实体一个 API 文件
├── src/components/      # 共享组件
├── src/pages/           # 每个模块一个页面目录
├── src/stores/          # 按实体拆分 store
├── src/hooks/           # 共享 hooks
├── src/utils/           # request、format 等
├── src/constants/       # 与后端对应枚举
├── Dockerfile
└── nginx.conf
database/
├── init.sql 或 migrations
docker-compose.yml
.env
.env.example
README.md
```

**严禁合并职责到单一文件**：不允许把多个实体的 model/repository/service/handler 写进同一个文件，也不允许前端把所有页面写在 App.vue / App.tsx 中。

### 10. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，在规范分层的基础上额外制造合理的“屎山”耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装。
   - 所有 handler、service、middleware 都必须引用 logger。
   - 日志格式字符串集中定义在 `internal/constants/log_templates.go`，至少 25 条模板。
   - 业务字段变更必须同步修改对应日志模板和调用处。

2. **异常信息分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message。
   - handler 必须再次包装 service 返回的错误。
   - 错误 message 中必须包含实体名、字段名、角色名。

3. **常量/工具类多处耦合**
   - `internal/util/formatters.go` 同时包含日期、状态文本、类型文本等格式化逻辑。
   - `internal/constants/messages.go` 同时包含接口返回文案、日志文案、错误提示文案。

4. **状态机必须跨多处定义**
   - 核心状态流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在。
   - 新增一个状态值需要修改至少 10 处。

5. **枚举多处重复定义且被多处引用**
   - 核心枚举必须在后端 constants、DTO、模型、日志模板、错误码、formatters 与前端 constants 中同时存在。
   - 新增一个枚举值必须修改至少 10 处文件。

6. **牵一发动全身验证标准**
   - 后续若要求“给核心实体新增一个字段/状态”，必须触达模型、DTO、constants、service、repository、handler、formatters、日志模板、错误码、前端类型与页面等 ≥ 10 个文件。

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 11. 部署要求

这是前后端分离项目。

原文端口分配：暴露端口（避免与其他项目冲突）：前端 8010，后端 3010，MongoDB 2803，Redis 6410

后端代码放在 `backend/`；前端代码放在 `frontend/`；数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排前端、后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=codelearn`
- `frontend/Dockerfile`：前端多阶段构建，再用 Nginx 托管
- `frontend/nginx.conf`：前端路由与 API 反向代理
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: codelearn`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-codelearn}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`（`/healthz`）
- 暴露端口：前端 `${FRONTEND_PORT:-80}:80`，后端 `${BACKEND_PORT:-8080}:8080`，数据库 `27017:27017`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=codelearn`
- 数据库连接相关：`DB_NAME=codelearn_db`、`DB_USER=codelearn_user`、`DB_PASSWORD=codelearn_pwd`
- 后端认证：`JWT_SECRET=change_me_to_a_long_random_string`
- 端口映射：`FRONTEND_PORT=80`、`BACKEND_PORT=8080`、`DB_PORT=27017`
- 缓存连接：`REDIS_ADDR=redis:6379`、`REDIS_PASSWORD=`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 启动命令）
- 访问地址、API 访问地址和 Swagger 文档地址
- 技术栈
- 项目目录结构
- 环境变量说明
- API 调用示例（curl）
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，前端写 `React + TypeScript，使用 Tailwind CSS 样式框架，Vite 构建工具`，后端写 `Go 1.22 + Gin + mongo-driver`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`，构建命令为 `go build ./...`
- 提供 curl 调用示例（含 JWT 请求头）
- 不要保留脚手架默认 README 内容
- 必须包含“枚举出现位置清单”，列出所有业务枚举在前后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 浏览器访问 `http://localhost:80` 能看到页面，且 `/api/...` 请求返回正常；后端 `http://localhost:8080/healthz` 返回 200
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 4 个，每个实体明确贯穿数据库 → 后端 model/repository/service/handler/router → 前端 api/store/page
7. 核心页面 ≥ 4 个，至少 2 个页面共用同一业务组件；后端接口 ≥ 6 个，至少 2 个复用同一 service
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 共享前端组件 ≥ 3 个，共享 hooks/utils ≥ 2 个
11. 后端中间件 ≥ 2 个
12. 文件结构强制清单已写入
13. 包含“严禁合并职责到单一文件”
14. 包含屎山代码设计要求

